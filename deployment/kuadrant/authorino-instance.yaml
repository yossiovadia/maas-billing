apiVersion: operator.authorino.kuadrant.io/v1beta1
kind: Authorino
metadata:
  name: authorino
  namespace: kuadrant-system
spec:
  listener:
    tls:
      enabled: false
  oidcServer:
    tls:
      enabled: false