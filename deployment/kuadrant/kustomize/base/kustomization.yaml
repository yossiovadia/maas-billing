apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: maas-base
  annotations:
    config.kubernetes.io/local-config: "true"

namespace: llm

resources:
  # Core Infrastructure
  - ../../00-namespaces.yaml
  - ../../01-kserve-config.yaml
  - ../../04-kuadrant-operator.yaml
  - ../../03-kuadrant-instance.yaml

commonLabels:
  app.kubernetes.io/name: maas-platform
  app.kubernetes.io/part-of: kuadrant-maas
  app.kubernetes.io/component: base
  app.kubernetes.io/managed-by: kustomize

commonAnnotations:
  config.kubernetes.io/origin: |
    path: deployment/kuadrant/kustomize/base/kustomization.yaml