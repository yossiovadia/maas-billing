# Optional: External access for remote key management
# Choose ONE of the options below based on your cluster setup TODO: pick one

# Option 1: OpenShift Route (for OpenShift clusters)
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: maas-api-route
  namespace: platform-services
spec:
  to:
    kind: Service
    name: maas-api
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect

# Option 2: Kubernetes Ingress (for other clusters)  
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: maas-api-ingress
  namespace: platform-services
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - maas-api.your-domain.com
    secretName: maas-api-tls
  rules:
  - host: maas-api.your-domain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: maas-api
            port:
              number: 80

# Option 3: LoadBalancer Service (for cloud providers)
---
apiVersion: v1
kind: Service
metadata:
  name: maas-api-external
  namespace: platform-services
spec:
  type: LoadBalancer
  selector:
    app: maas-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
