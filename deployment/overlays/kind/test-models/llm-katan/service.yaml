apiVersion: v1
kind: Service
metadata:
  name: llm-katan
  labels:
    app: llm-katan
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: llm-katan
